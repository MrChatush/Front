<Window x:Class="BroMessenger.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BroMessenger"
        mc:Ignorable="d"
        Title="Авторизация" 
        Height="400" 
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0 0 0 15"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#FF1E1E1E">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="Black" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="#FF1A1A1A" CornerRadius="10 10 0 0">
                <TextBlock Text="Пиводжер" 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="Bold"
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center"/>
            </Border>

            <!-- Основное содержимое -->
            <StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Center">
                <TextBlock Text="Вход" 
                           Foreground="White" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Margin="0 0 0 20"
                           HorizontalAlignment="Center"/>

                <Grid Margin="0 0 0 15" Height="37">
                    <TextBox x:Name="UsernameBox" Text="Имя пользователя" 
                             Foreground="#FFAAAAAA" Margin="0"/>
                    <TextBlock x:Name="UsernamePlaceholder" Text="Имя пользователя" 
                               Foreground="#FFAAAAAA" Margin="10,8,0,0" 
                               IsHitTestVisible="False"
                               Visibility="{Binding ElementName=UsernameBox, Path=Text.IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </Grid>

                <Grid Margin="0 0 0 15" Height="37">
                    <PasswordBox x:Name="PasswordBox" Foreground="White"
                                PasswordChar="•" Margin="0"
                                PasswordChanged="PasswordBox_PasswordChanged"/>
                    <TextBlock x:Name="PasswordPlaceholder" Text="Пароль" 
                               Foreground="#FFAAAAAA" Margin="10,8,0,0" 
                               IsHitTestVisible="False"/>
                </Grid>
            </StackPanel>

            <!-- Кнопка входа -->
            <Border Grid.Row="2" Background="#FF1A1A1A" CornerRadius="0 0 10 10" Padding="20">
                <Button x:Name="LoginButton" Content="Продолжить" 
                        Background="#FF1E6DD9" FontWeight="Bold"
                        Click="LoginButton_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>