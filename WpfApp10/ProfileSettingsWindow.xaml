<Window x:Class="WpfApp10.ProfileSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Настройки профиля" 
        Height="450" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <!-- Светло-серый цвет для текста -->
        <SolidColorBrush x:Key="LightGrayTextBrush" Color="#FFAAAAAA"/>

        <!-- Стиль для DatePicker -->
        <Style x:Key="CustomDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Padding="{TemplateBinding Padding}"/>
                                <Button x:Name="PART_Button"
                                        Grid.Column="1"
                                        Content="🗓"
                                        Foreground="{TemplateBinding Foreground}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Margin="5,0,0,0"
                                        ClickMode="Press">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Popup x:Name="PART_Popup"
                                       PlacementTarget="{Binding ElementName=PART_Button}"
                                       Placement="Bottom"
                                       AllowsTransparency="True"
                                       StaysOpen="False">
                                    <Border Background="{DynamicResource ControlBackgroundBrush}"
                                            BorderBrush="{DynamicResource LightGrayTextBrush}"
                                            BorderThickness="1">
                                        <Calendar x:Name="PART_Calendar"
                                                  Background="{DynamicResource ControlBackgroundBrush}"
                                                  Foreground="{DynamicResource TextForegroundBrush}"
                                                  BorderThickness="0"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource LightGrayTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!-- Аватар -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Border Width="100" Height="100" CornerRadius="50" 
                    Background="{DynamicResource ControlBackgroundBrush}"
                    BorderBrush="{DynamicResource LightGrayTextBrush}"
                    BorderThickness="2">
                <Image Source="{Binding AvatarImage}" Stretch="UniformToFill"/>
            </Border>
            <Button Content="Изменить аватар" 
                    Margin="0,10,0,0"
                    Command="{Binding ChangeAvatarCommand}"/>
        </StackPanel>

        <!-- Никнейм -->
        <TextBlock Grid.Row="1" Text="Никнейм:"/>
        <TextBox Grid.Row="2" Text="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Дата рождения -->
        <TextBlock Grid.Row="3" Text="Дата рождения:"/>
        <DatePicker Grid.Row="4" SelectedDate="{Binding BirthDate, Mode=TwoWay}" Style="{StaticResource CustomDatePickerStyle}"/>


        <!-- Кнопка сохранения -->
        <Button Grid.Row="5" 
                Content="Сохранить" 
                HorizontalAlignment="Right"
                Width="100"
                Command="{Binding SaveCommand}" Cursor="Hand"/>
    </Grid>
</Window>